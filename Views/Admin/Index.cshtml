@model solder.ViewModels.CommonSolderViewModel
@{
    ViewBag.Title = "All solders";
}        
<a asp-action="CreateSolder">Add solder</a>
<table class="table">
    @foreach (var item in Model.Solders)
    {
        <tr>
            <td>@item.Name</td>
            <td><a asp-action="Details" asp-controller="Admin" asp-route-id="@item.Id">Подробнее</a> | 
                <a asp-action="Edit" asp-controller="Admin" asp-route-id="@item.Id">Изменить</a> | 
                <a asp-action="Delete" asp-controller="Admin" asp-route-id="@item.Id">Удалить</a>
            </td>
        </tr>
    }    
</table>
<hr/>
<a asp-action="CreateSolderType">Add SolderType</a>
<table class="table">
    @foreach (var item in Model.SolderTypes)
    {
        <tr>
            <td>@item.Name</td>
            <td><a asp-action="Details" asp-controller="Admin" asp-route-id="@item.Id">Подробнее</a> | 
                <a asp-action="Edit" asp-controller="Admin" asp-route-id="@item.Id">Изменить</a> | 
                <a asp-action="Delete" asp-controller="Admin" asp-route-id="@item.Id">Удалить</a>
            </td>
        </tr>
    }    
</table>
<hr/>
<a asp-action="Create">Add Product</a>
<table class="table">
    @foreach (var item in Model.Products)
    {
        <tr>
            <td>@item.Name</td>
            <td><a asp-action="Details" asp-controller="Admin" asp-route-id="@item.Id">Подробнее</a> | 
                <a asp-action="Edit" asp-controller="Admin" asp-route-id="@item.Id">Изменить</a> | 
                <a asp-action="Delete" asp-controller="Admin" asp-route-id="@item.Id">Удалить</a>
            </td>
        </tr>
    }    
</table>